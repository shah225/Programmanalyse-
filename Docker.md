# Docker

There a two Dockerfiles:

- Dockerfile: builds the project from scratch
- DockerfileDev: Provides development environment

To use the development environment first execute

```
startDevEnv.sh
```

Inside the development environment you can build and run the project using

```
runDevEnv.sh
```

This enables incremental building inside the development environment.